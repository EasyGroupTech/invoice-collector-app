// Placeholder — ic-core's real entry point (Electron main/preload/renderer wiring) lands in
// later implementation phases (docs/implementation-plan.md 1.5+ in the invoice-collector repo).
// This file exists only to give the monorepo scaffold something real to typecheck/build/test
// against.
export const PACKAGE_NAME = 'ic-core';
