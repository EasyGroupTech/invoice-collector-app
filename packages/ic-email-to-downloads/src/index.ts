// Placeholder — the real Graph Mail source (discover()/fetchContent(), field-rule capture) and
// local-folder destination land in implementation phase 1.14/1.15 (see
// docs/implementation-plan.md in the invoice-collector repo). This file exists only to give the
// monorepo scaffold something real to typecheck/build/test against.
export const PACKAGE_NAME = 'ic-email-to-downloads';
