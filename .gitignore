node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Generated by generate-sbom. Not regenerated by any real build/CI step yet (that's
# implementation-plan.md phase 1.16) — ic-core's and the SDK's own are committed anyway
# (phase 1.12c's SBOM/licenses screen needs real data to read, not nothing), kept current by
# hand via `npm run generate-sbom:ic-core`/`generate-sbom:sdk` until 1.16 automates it. Anything
# else matching this pattern (an ad-hoc test plugin's own, etc.) stays ignored.
*.cdx.json
!/packages/ic-core/sbom.cdx.json
!/packages/invoice-collector-plugin-sdk/sbom.cdx.json
