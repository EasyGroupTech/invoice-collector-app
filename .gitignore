node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# Generated by generate-sbom during manual/local verification — not yet wired into any package's
# real build (that's implementation-plan.md phase 1.16), so nothing should commit one yet.
*.cdx.json
